import 'package:flutter/material.dart';

class AppColors {
 

  // ── Base ──
  static const Color black = Color(0xFF100E09);
  static const Color white = Color(0xFFFFFFFF);

  // ── Background ──
  static const Color scaffoldBackground = Color(0XFFEBEBEB);
  static const Color pageBackground = white;      

  // ── Text ──
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF333333);       
           
  static const Color textError = Color(0XFFFF5454);      
          
  static const Color textLink = Color(0xFF2873F0);

  // ── Input ──
  static const Color inputBorder = Color(0XFF000000);     
  
  static const Color inputFill = white;

  // ── AppBar ──
  static const Color appBarBg = black;
  static const Color appBarText = white;

 

  // ── OTP Box ──
  static const Color otpBorder = Color(0xFF100E09);            
  static const Color otpFocused = Color(0XFF100E09);
}